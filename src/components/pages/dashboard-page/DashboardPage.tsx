import type { FC } from "react";

export const DashboardPage: FC = () => {
    return (
        <div>
        <h1>Dashboard</h1>
        <p>Welcome to the dashboard page!</p>
        </div>
    );
}